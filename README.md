# QR-Attendance-Tracker
A simple web application that scans QR codes to mark attendance at an event. It reads participant details from the QR code, marks them as present, prevents duplicate entries, and shows the attendance status on the screen. Built using HTML, CSS, and JavaScript.
